name: example
description: Demonstrates how to use the isa_kit package.
publish_to: 'none'

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.1
  fl_chart: ^0.68.0
  maplibre_gl: ^0.22.0
  
  isa_kit:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
